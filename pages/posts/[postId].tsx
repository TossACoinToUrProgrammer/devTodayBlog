import React from 'react';
import MainLayout from '../../components/MainLayout';

const Post = ({ post }) => {
    return (
        <MainLayout>
            Post
        </MainLayout>
    );
};

export default Post;